# v6.2.0 — Tribute Webhook Deployment Record

**Worker:** `rotationtv-live-ai-clones` (account 947b01a53876bee16fa0e8360c880aca)
**Version:** 35 — `d0566fa5-e74f-4625-b7ac-5f8350db904c`
**Uploaded:** 2026-07-18 via Cloudflare API (version upload verified, all 26 bindings resolved)
**Status:** UPLOADED, pending traffic flip. Live is still v34 (`edb0c136`, 6.1.0).

## Flip command (when CF API recovers or via dashboard)
```
npx wrangler versions deploy d0566fa5-e74f-4625-b7ac-5f8350db904c@100% -y
```
Dashboard: Workers & Pages → rotationtv-live-ai-clones → Deployments → pick version 35 → Deploy.

## What v6.2.0 adds
- `POST /api/tribute/webhook` — Tribute payment webhook
  - HMAC-SHA256 signature verification (`X-Tribute-Signature` header, `sha256=` prefix tolerated) against `env.TRIBUTE_WEBHOOK_SECRET` (fallback `env.TRIBUTE`) — fail-closed 503 if unset
  - Idempotency via `KV_SPEND` (`tribute_evt_{event_id}`, 7-day TTL)
  - 80/15/5 revenue split computation (creator/platform/agency)
  - Ledger insert into `public.tribute_events` (Supabase, RLS locked, service-role only)
  - Premium tier upgrade on paid events (`profiles.tier = 'premium'` by telegram_id)
  - Telegram welcome message via `TELEGRAM_BOT_TOKEN_MAIN`
- `GET /api/tribute/health` — config status check
- Version marker bumped 6.1.0 → 6.2.0
- All other routes unchanged (additive patch only)

## Post-flip steps
1. `wrangler secret put TRIBUTE_WEBHOOK_SECRET` (value from Tribute Creator Dashboard → API Keys)
2. Tribute dashboard webhook URL: `https://rotationtv-live-ai-clones.rotationtimmy.workers.dev/api/tribute/webhook`
3. Verify: `GET /api/tribute/health` → `hmac_configured: true`

## Rollback
```
npx wrangler versions deploy edb0c136-aa34-4a50-88aa-0ff248db427a@100% -y
```

## Related migrations (applied 2026-07-18)
- `fix_profiles_rls_recursion_is_admin` — profiles RLS infinite recursion fix
- `create_tribute_events_table` — webhook ledger (RLS enabled, no public policies)
