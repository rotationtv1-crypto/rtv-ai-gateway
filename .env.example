# RotationTV — Environment Template
# Copy to .env (local) or configure in GitHub Actions Secrets + Worker secrets.
# NEVER commit filled values.

# --- Supabase: Rotation Erotica project (zzybjoowhkwuomnpixuy) ---
SUPABASE_URL=https://zzybjoowhkwuomnpixuy.supabase.co
SUPABASE_ANON_KEY=          # public, client-safe (RLS-gated)
SUPABASE_SERVICE_ROLE_KEY=  # STRICTLY PRIVATE — server/worker only, bypasses RLS

# --- Cloudflare (CI deploy) ---
CLOUDFLARE_API_TOKEN=       # Workers:Edit scope
CLOUDFLARE_ACCOUNT_ID=947b01a53876bee16fa0e8360c880aca

# --- Telegram ---
TELEGRAM_BOT_TOKEN=         # via @BotFather; rotate any token ever pasted in chat
